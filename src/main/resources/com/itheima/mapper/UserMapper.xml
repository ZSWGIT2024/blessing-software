<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itheima.mapper.UserMapper">
    <update id="updateUser">
        UPDATE user
        <set>
        <if test="username != null">username = #{username},</if>
        <if test="nickname != null">nickname = #{nickname},</if>
        <if test="gender != null">gender = #{gender},</if>
        <if test="email != null">email = #{email},</if>
        <if test="birthday != null">birthday = #{birthday},</if>
        <if test="hobbies != null">hobbies = #{hobbies},</if>
        <if test="favoriteThings != null">favorite_things = #{favoriteThings},</if>
        <if test="bloodType != null">blood_type = #{bloodType},</if>
        <if test="constellation != null">constellation = #{constellation},</if>
        <if test="bio != null">bio = #{bio},</if>
        update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <!-- 分页查询用户 -->
    <select id="findPage" resultType="com.itheima.pojo.User">
        SELECT * FROM user
        <where>
            <if test="keyword != null and keyword != ''">
                (username LIKE CONCAT('%', #{keyword}, '%')
                OR phone LIKE CONCAT('%', #{keyword}, '%')
                OR nickname LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="vipType != null">
                AND vip_type = #{vipType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="minLevel != null">
                AND level >= #{minLevel}
            </if>
            <if test="maxLevel != null">
                <![CDATA[
            AND level <= #{maxLevel}
            ]]>
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 统计用户数量 -->
    <select id="countUsers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM user
        <where>
            <if test="keyword != null and keyword != ''">
                (username LIKE CONCAT('%', #{keyword}, '%')
                OR phone LIKE CONCAT('%', #{keyword}, '%')
                OR nickname LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="vipType != null">
                AND vip_type = #{vipType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>
    <select id="selectByIds" resultType="com.itheima.pojo.User">
        SELECT * FROM user WHERE id IN
        <foreach collection="ids" item="id" index="index" open="(" close=")" separator="," >#{id}</foreach>
    </select>

    <select id="selectCurrentFrameId" resultType="Integer">
        SELECT frame_id FROM user_avatar_frame
        WHERE user_id = #{userId} AND is_using = 1
            LIMIT 1
    </select>


</mapper>